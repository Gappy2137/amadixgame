//window_set_size(view_width*window_scale, view_height*window_scale);

//surface_resize(application_surface, view_width*window_scale, view_height*window_scale);



camLeft = camX;
camTop = camY;
camRight = camX + view_width;
camBottom = camY + view_height;